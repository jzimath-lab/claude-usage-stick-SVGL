{
  "name": "estacao-cotas",
  "private": true,
  "description": "LAN GET /cotas for the 3.5\" stick (ZYN-568/569). Actions via G1; Codex via CodexBar / wham / app-server.",
  "type": "commonjs",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bonjour-service": "^1.3.0"
  }
}
