# ───────── Segredos — NUNCA commitar ─────────
# .env: token OAuth do Claude
# .mcp.json: contém chave da OpenAI no ambiente do autor
.env
.env.*
.mcp.json
secrets.h
*.token

# ───────── Config local de ferramentas ─────────
# .claude/: config/skills locais do Claude Code (não faz parte do projeto)
.claude/
.vscode/
.idea/

# ───────── Build / artefatos ─────────
firmware/*/build/
.pio/
*.bin
*.elf
*.hex

# ───────── Python (tools/) ─────────
__pycache__/
*.py[cod]
.venv/
venv/

# ───────── SO ─────────
.DS_Store
Thumbs.db

# credenciais do bridge Codex (device)
firmware/claude_stick/codex_secrets.h
firmware/claude_stick/github_secrets.h
firmware/tests/cotas_parse_test
firmware/tests/vps_creds_test
firmware/tests/cotas_mapa_test
